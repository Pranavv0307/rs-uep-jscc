# local env
.venv/
venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/
notebooks/*-checkpoint.ipynb
pynbs/*-checkpoint.ipynb

# datasets — never commit these
data/cifar-10-batches-py/
data/cifar-10-python.tar.gz
cifar10_data/
data/*.zip

# checkpoints, logs, generated outputs — never commit these
results/
*.pth
*.pt
*.ckpt

# test cache
.pytest_cache/

# W&B local run mirrors — never commit these, regenerate every run
wandb/
wandb_logs/

# OS/editor junk
.DS_Store
.vscode/

week1_execution_plan.md

# local untracked files
amritape/
images/
scratch_eval.py